import { DisplayHoursPipe } from './display-hours.pipe';

describe('DisplayHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
